Requires:
 - C++17 capable build system (Visual Studio, XCode, Make, etc.)

Building on Windows
 - Run `Project/GENERATE_PROJECTS_VS20xx.bat`,
 	depending on your installed Visual Studio version

Building on Mac OS
 - Run `cd ./Project && bash GENERATE_PROJECTS_XCODE.sh`